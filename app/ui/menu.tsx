'use client';

import { useState } from "react";

export default function Menu() {
  const [showMenu, setShowMenu] = useState('hidden');

  const handleClick  (e) =>  {
    setShowMenu('show');
  }
  return (
    
  )
  
}