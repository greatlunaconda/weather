//import  {getPlaceFromCookie, savePlaceToCookie, PlaceData } from cookies.ts 

//const handleSave = async (place: PlaceData) => {
//    if (place.name && place.lat && place.lon) {
  //    const places = await getPlaceFromCookie();

    /*places.unshift(place);//
        await savePlaceToCookie(places);
        onPlaceSelect?.(place);
        alert('Place saved successfully!');
      }
    }
  };*/